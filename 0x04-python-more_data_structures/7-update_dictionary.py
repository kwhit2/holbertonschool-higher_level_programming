#!/usr/bin/python3
def update_dictionary(a_dictionary, key, value):
    a_dictionary[key] = value
    return a_dictionary
#  if the key doesn't exist it will be created
#  if the key does exist it will be overwritten/replaced
