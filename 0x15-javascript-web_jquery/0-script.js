// JavaScript script that updates the text color of the <header> element to
// red (#FF0000): must use document.querySelector to select the HTML tag
document.querySelector('header').style = 'color: red;';
